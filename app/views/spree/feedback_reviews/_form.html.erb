<%= render 'spree/feedback_reviews/summary', review: review %>
<%= form_for (@feedback_review ||= review.feedback_reviews.build), url: feedback_reviews_path(review), method: :post, remote: true do |f| %>
  <% unless @feedback_review.errors.empty? %>
    <div class="error"><%= @feedback_review.errors[:rating] %></div>
  <% end %>
  <%= Spree.t(:was_this_review_helpful) %>
<!--    <% for i in 1..Spree::Reviews::Config.stars %>
    <%= radio_button_tag 'feedback_review[rating]', Spree.t(:star, count: i), false, class: 'star' %>
  <% end %>  -->
  <button class="feedback-review say_yes btn btn-default" name="feedback_review[rating]" value="5">
    <%= Spree.t(:say_yes) %>
  </button>
   <button class="feedback-review say_no btn btn-default" name="feedback_review[rating]" value="1" >
    <%= Spree.t(:say_no) %>
  </button>
<% end %>
